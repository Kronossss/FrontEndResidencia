import { RealceTermoPipe } from './realce-termo.pipe';

describe('RealceTermoPipe', () => {
  it('create an instance', () => {
    const pipe = new RealceTermoPipe();
    expect(pipe).toBeTruthy();
  });
});
