import { VeiculoEstiloDirective } from './veiculo-estilo.directive';

describe('VeiculoEstiloDirective', () => {
  it('should create an instance', () => {
    const directive = new VeiculoEstiloDirective();
    expect(directive).toBeTruthy();
  });
});
