import { CategoriaEstiloDirective } from './categoria-estilo.directive';

describe('CategoriaEstiloDirective', () => {
  it('should create an instance', () => {
    const directive = new CategoriaEstiloDirective();
    expect(directive).toBeTruthy();
  });
});
